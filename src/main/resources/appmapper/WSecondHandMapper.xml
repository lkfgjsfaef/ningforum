<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.dao.WSecondHandMapper">

    <resultMap id="PostVOMap" type="PostVO">
        <id property="postId" column="post_id"/>
        <result property="userId" column="user_id"/>
        <result property="username" column="username"/>
        <result property="avatar" column="avatar"/>
        <result property="categoryId" column="category_id"/>
        <result property="categoryName" column="category_name"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="price" column="price"/>
        <result property="image1" column="image1"/>
        <result property="image2" column="image2"/>
        <result property="image3" column="image3"/>
        <result property="status" column="status"/>
        <result property="viewCount" column="view_count"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectSecondHandPosts" resultMap="PostVOMap" parameterType="map">
        SELECT DISTINCT
        p.post_id,
        p.user_id,
        p.category_id,
        p.title,
        p.content,
        p.price,
        p.image1,
        p.image2,
        p.image3,
        p.status,
        p.view_count,
        p.create_time,
        u.username,
        u.avatar,
        c.name as category_name
        FROM post p
        LEFT JOIN user u ON p.user_id = u.user_id
        LEFT JOIN category c ON p.category_id = c.category_id
        <if test="tagId != null">
            LEFT JOIN post_tag pt ON p.post_id = pt.post_id
        </if>
        <where>
            AND p.category_id = #{categoryId} AND p.status IN (0, 1)
            <if test="tagId != null">
                AND pt.tag_id = #{tagId}
            </if>
        </where>
        <choose>
            <when test="sortType == 'newest'">
                ORDER BY p.create_time DESC
            </when>
            <when test="sortType == 'hottest'">
                ORDER BY p.view_count DESC, p.create_time DESC
            </when>
            <otherwise>
                ORDER BY p.create_time DESC
            </otherwise>
        </choose>
        LIMIT #{start}, #{size}
    </select>

    <select id="countSecondHandPosts" resultType="int" parameterType="map">
        SELECT COUNT(DISTINCT p.post_id)
        FROM post p
        <if test="tagId != null">
            LEFT JOIN post_tag pt ON p.post_id = pt.post_id
        </if>
        <where>
            AND p.category_id = #{categoryId} AND p.status IN (0, 1)
            <if test="tagId != null">
                AND pt.tag_id = #{tagId}
            </if>
        </where>
    </select>

    <select id="selectSecondHandPostById" resultMap="PostVOMap">
        SELECT
        p.post_id,
        p.user_id,
        p.category_id,
        p.title,
        p.content,
        p.price,
        p.image1,
        p.image2,
        p.image3,
        p.status,
        p.view_count,
        p.create_time,
        u.username,
        u.avatar,
        c.name as category_name
        FROM post p
        LEFT JOIN user u ON p.user_id = u.user_id
        LEFT JOIN category c ON p.category_id = c.category_id
        WHERE p.post_id = #{postId} AND p.category_id = 3 AND p.status IN (0, 1)
    </select>

    <update id="incrementViewCount">
        UPDATE post
        SET view_count = view_count + 1
        WHERE post_id = #{postId}
    </update>

    <update id="deletePost">
        UPDATE post
        SET status = 2
        WHERE post_id = #{postId}
        AND user_id = #{userId}
        AND category_id = 3
        AND status != 2
        AND status != 3
    </update>

</mapper>
